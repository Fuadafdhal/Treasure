<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
        <item name="backgroundColor">@color/windowBackgoround</item>
    </style>

    <style name="ToolbarTheme" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="android:textColorPrimary">@color/colorTextLabel</item>
        <item name="actionMenuTextColor">@color/colorTextItemToolbar</item>
        <item name="android:textColorSecondary">@color/colorTextHint</item>
    </style>
</resources>